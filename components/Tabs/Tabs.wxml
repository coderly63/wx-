<view class="tabs">
  <view
    class="tab_item {{ index === currentIndex ? 'active' : '' }}"
    wx:for="{{ tabs }}"
    wx:key="*this"
    bindtap="handleItemTap"
    data-index="{{ index }}"
  >
    {{ item }}
  </view>
</view>
